program FactorialRecursivo;
var
int : num;

function int factorial(int : x) {
    if (x <= 1){
        return 1;
    }
    return x * factorial(x - 1);
}

main() {
    print("Write number to get factorial");
    input(num);
    print(factorial(num));
}